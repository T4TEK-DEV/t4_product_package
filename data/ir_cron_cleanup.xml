<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Cron "Dọn dẹp phiếu Lắp Ráp/Định Danh nháp rỗng" ĐÃ GỠ.
         Lý do: phiếu Định Danh tạo tự động (từ phiếu điều chuyển đi định danh)
         bắt đầu ở Nháp RỖNG (kỹ thuật chưa quét) — là việc cần làm; cron dọn
         nháp rỗng > 24h sẽ xóa mất chúng. Bản ghi cron cũ được xóa qua
         migration 1.0.21. Method `_cron_cleanup_empty_drafts` giữ lại (không
         còn cron gọi). -->
</odoo>